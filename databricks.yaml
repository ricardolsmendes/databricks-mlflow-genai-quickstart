bundle:
  name: mlflow-genai-quickstart

include:
  - resources/*.yaml

sync:
  paths:
    - src

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ""

variables:
  environment:
    description: Deployment environment (dev, staging, qa, prod)
    default: dev
    type: string
