bundle:
  name: mlflow-genai-quickstart

include:
  - resources/*.yaml

variables:
  environment:
    description: Deployment environment (dev, staging, qa, prod)
    default: dev
    type: string

sync:
  paths:
    - src

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ""
